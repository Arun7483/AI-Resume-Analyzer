services:

  # =====================================================
  # BACKEND - SPRING BOOT
  # =====================================================

  - type: web
    name: resume-analyzer-api
    runtime: docker
    rootDir: backend

    healthCheckPath: /actuator/health

    envVars:
      - key: PORT
        value: 10000

      - key: SPRING_PROFILES_ACTIVE
        value: prod

      # -------------------------------------------------
      # DATABASE
      # -------------------------------------------------

      - key: DATABASE_URL
        sync: false

      - key: DB_USERNAME
        sync: false

      - key: DB_PASSWORD
        sync: false

      # -------------------------------------------------
      # JWT
      # -------------------------------------------------

      - key: JWT_SECRET
        sync: false

      - key: JWT_EXPIRATION
        value: 86400000

      # -------------------------------------------------
      # CORS
      # -------------------------------------------------

      - key: CORS_ALLOWED_ORIGIN
        sync: false

      - key: BACKEND_URL
        sync: false

      - key: FRONTEND_URL
        sync: false

      # -------------------------------------------------
      # GROQ
      # -------------------------------------------------

      - key: GROQ_API_KEY
        sync: false

      - key: ADZUNA_APP_ID
        sync: false

      - key: ADZUNA_APP_KEY
        sync: false

      - key: GOOGLE_CLIENT_ID
        sync: false

      - key: RESEND_API_KEY
        sync: false

      - key: MAIL_FROM
        sync: false


  # =====================================================
  # FRONTEND - ANGULAR
  # =====================================================

  - type: web
    name: resume-pulse-frontend-bbep
    runtime: static
    rootDir: frontend

    buildCommand: npm install && node -e "const fs=require('fs'); const p='src/index.html'; let s=fs.readFileSync(p,'utf8'); s=s.replace('__RESUME_API_URL__', process.env.API_URL || ''); s=s.replace('__GOOGLE_CLIENT_ID__', process.env.GOOGLE_CLIENT_ID || ''); fs.writeFileSync(p,s);" && npm run build

    staticPublishPath: ./dist/resume-pulse/browser

    routes:
      - type: rewrite
        source: /*
        destination: /index.html

    envVars:
      - key: API_URL
        sync: false

      - key: GOOGLE_CLIENT_ID
        sync: false
