# ResumePulse frontend

Standalone Angular 19 client using signals, Tailwind CSS, Lucide icons, and typed API services targeting `http://localhost:8080/api/v1`.

```bash
npm install
npm start
```

Representative analysis data is included for previewing the full dashboard before the Spring Boot API is available.
